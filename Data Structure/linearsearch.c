#include<stdio.h>

int main()
{
    int n,a[6],i;
     printf("Enter number element:\n");
    scanf("%d",&n);

    printf("Enter Elements:");
    for(i=0;i<=6;i++){
        scanf("%d",a[i]);
     }

   if
   {
      printf("Enter element to search:\n");
   }
   else
   {
        printf("not found");
   }     
return 0;
}
